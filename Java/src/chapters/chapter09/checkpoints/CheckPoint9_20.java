package chapters.chapter09.checkpoints;

public class CheckPoint9_20 {
    //Accessor method is for retrieving private data value and mutator method is for changing private data value.
    // The naming convention for accessor method is getDataFieldName() for non-boolean values and isDataFieldName()
    // for boolean values. The naming convention for mutator method is setDataFieldName(value).
}
