package chapters.chapter09.checkpoints;

public class CheckPoint9_11 {
    //(a) There is no such constructor ShowErrors(int) in the ShowErrors class.
    //(b) x() is not a method in the ShowErrors class.
    //(c) The program compiles fine, but it has a runtime error because variable c is null when the println statement is executed.
    //(d) new C(5.0) does not match any constructors in class C.
    // The program has a compilation error because class C does not have a constructor with a double argument.
}
