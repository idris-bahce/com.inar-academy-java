package chapters.chapter11.checkpoints;

public class CheckPoint11_30 {
    //a) we use Double class
    //b) we use add method
    //c) we use insert(index,object) method
    //d) we use size() method
    //e) we use remove(object) method
    //f) we use remove(name.size - 1) method
    //g) we use contains() method
    //h) we use get(index) method
}
