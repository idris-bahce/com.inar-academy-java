package chapters.chapter11.checkpoints;

public class CheckPoint11_39 {
    //if it was space, we can access to A because they are in the same package
    //if it was private we would not access it.
    //if it was protected we could access it
}
